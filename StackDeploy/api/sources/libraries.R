# Libraries required--------------------------------

require(plumber)    # for API working
require(DBI)        # for database connectivity
require(RJDBC)      # for database connectivity
require(promises)   # for async programming
require(jsonlite)   # for json formats
require(sodium)     # for hashing
require(dplyr)      # for data maipulation
require(lubridate)  # for handling dates
require(mongolite)  # for logging database connectivity
require(sodium)     # for password hashing
require(openssl)    # for JWT
require(jose)       # for JWT